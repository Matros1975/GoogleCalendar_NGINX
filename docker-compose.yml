# Multi-MCP Deployment with NGINX Proxy - Production Configuration
# Refactored structure supporting multiple MCP servers behind NGINX
# Each MCP server is in its own subdirectory under Servers/

networks:
  # Internal network for MCP services
  mcp-internal:
    driver: bridge
    internal: false  # Allow external access for OAuth callbacks and Let's Encrypt
    ipam:
      config:
        - subnet: 172.20.0.0/16
  
  # External web traffic network
  web:
    driver: bridge
    external: false
    ipam:
      config:
        - subnet: 172.21.0.0/16

services:
  # Google Calendar MCP Server
  calendar-mcp:
    build: ./Servers/GoogleCalendarMCP
    container_name: calendar-mcp
    restart: unless-stopped
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    # Internal network only - no port exposure to host except OAuth
    networks:
      - mcp-internal
    
    # Expose OAuth ports for authentication (3500-3505)
    ports:
      - "3500:3500"
      - "3501:3501"
      - "3502:3502"
      - "3503:3503"
      - "3504:3504"
      - "3505:3505"
    
    # Environment configuration
    environment:
      - TRANSPORT=${MCP_TRANSPORT:-http}
      - HOST=${GOOGLE_MCP_HOST:-0.0.0.0}
      - PORT=${GOOGLE_MCP_PORT:-3000}
      - NODE_ENV=${NODE_ENV:-production}
      - GOOGLE_OAUTH_CREDENTIALS=${GOOGLE_OAUTH_CREDENTIALS:-/app/gcp-oauth.keys.json}
      - GOOGLE_ACCOUNT_MODE=${GOOGLE_ACCOUNT_MODE:-normal}
    
    # OAuth credentials and token storage
    volumes:
      - ./gcp-oauth.keys.json:/app/gcp-oauth.keys.json:ro
      - calendar-tokens:/home/nodejs/.config/google-calendar-mcp
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
          cpus: "0.5"
    
    # Enhanced Security Configuration
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Read-only root filesystem for security
    read_only: true
    
    # Drop all capabilities and add only necessary ones
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    
    # Temporary filesystems for writes
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=100m
      - /app/logs:rw,noexec,nosuid,nodev,size=50m
    
    # Health check
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://0.0.0.0:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Gmail MCP Server
  gmail-mcp:
    build: ./Servers/GmailMCP
    container_name: gmail-mcp
    restart: unless-stopped
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    # Internal network only - no port exposure to host except OAuth
    networks:
      - mcp-internal
    
    # Expose OAuth ports for authentication (3506-3511)
    ports:
      - "3506:3506"
      - "3507:3507"
      - "3508:3508"
      - "3509:3509"
      - "3510:3510"
      - "3511:3511"
    
    # Environment configuration
    environment:
      - TRANSPORT=${MCP_TRANSPORT:-http}
      - HOST=${GMAIL_MCP_HOST:-0.0.0.0}
      - PORT=${GMAIL_MCP_PORT:-3001}
      - NODE_ENV=${NODE_ENV:-production}
      - GMAIL_OAUTH_CREDENTIALS=${GMAIL_OAUTH_CREDENTIALS:-/app/gcp-oauth.keys.json}
      - GOOGLE_OAUTH_CREDENTIALS=${GOOGLE_OAUTH_CREDENTIALS:-/app/gcp-oauth.keys.json}
      - GOOGLE_ACCOUNT_MODE=${GOOGLE_ACCOUNT_MODE:-normal}
    
    # OAuth credentials and token storage
    volumes:
      - ./gcp-oauth.keys.json:/app/gcp-oauth.keys.json:ro
      - gmail-tokens:/home/nodejs/.config/gmail-mcp
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
          cpus: "0.5"
    
    # Enhanced Security Configuration
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Read-only root filesystem for security
    read_only: true
    
    # Drop all capabilities and add only necessary ones
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    
    # Temporary filesystems for writes
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=100m
      - /app/logs:rw,noexec,nosuid,nodev,size=50m
    
    # Health check - simple process check
    healthcheck:
      test: ["CMD-SHELL", "ps aux | grep -v grep | grep node || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # TopDesk MCP Server (old buggy implementation) - DISABLED
  # Replaced with TopDeskCustomMCP due to FastMCP parameter validation bugs
  # topdesk-mcp:
  #   build: ./Servers/TopDeskMCP
  #   container_name: topdesk-mcp
  #   restart: unless-stopped
  #   
  #   # Load centralized environment configuration
  #   env_file:
  #     - .env
  #   
  #   # Internal network only
  #   networks:
  #     - mcp-internal
  #   
  #   # Environment configuration
  #   environment:
  #     - TOPDESK_MCP_TRANSPORT=${TOPDESK_MCP_TRANSPORT:-streamable-http}
  #     - TOPDESK_MCP_HOST=${TOPDESK_MCP_HOST:-0.0.0.0}
  #     - TOPDESK_MCP_PORT=${TOPDESK_MCP_PORT:-3002}
  #     - TOPDESK_URL=${TOPDESK_URL:-https://example.topdesk.net}
  #     - TOPDESK_USERNAME=api_aipilots
  #     - TOPDESK_PASSWORD=7w7j6-ytlqt-wpcbz-ywu6v-remw7
  #     - TOPDESK_API_TOKEN=${TOPDESK_API_TOKEN:-example_token}
  #   
  #   # Data storage
  #   volumes:
  #     - topdesk-data:/app/data
  #   
  #   # Resource limits
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 512M
  #         cpus: "1.0"
  #       reservations:
  #         memory: 256M
  #         cpus: "0.5"
  #   
  #   # Enhanced Security Configuration
  #   security_opt:
  #     - no-new-privileges:true
  #     - apparmor:unconfined
  #   
  #   # Read-only root filesystem for security
  #   read_only: true
  #   
  #   # Drop all capabilities and add only necessary ones
  #   cap_drop:
  #     - ALL
  #   cap_add:
  #     - NET_BIND_SERVICE
  #   
  #   # Temporary filesystems for writes
  #   tmpfs:
  #     - /tmp:rw,noexec,nosuid,nodev,size=100m
  #     - /app/logs:rw,noexec,nosuid,nodev,size=50m
  #   
  #   # Health check
  #   healthcheck:
  #     test: ["CMD-SHELL", "ps aux | grep -v grep | grep 'topdesk_mcp.main' > /dev/null || exit 1"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s

  # TopDesk Custom MCP Server (new direct implementation)
  topdesk-custom-mcp:
    build: ./Servers/TopDeskCustomMCP
    container_name: topdesk-custom-mcp
    restart: unless-stopped
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    # Internal network only
    networks:
      - mcp-internal
    
    # Environment configuration
    environment:
      - TOPDESK_BASE_URL=${TOPDESK_BASE_URL:-https://pietervanforeest-test.topdesk.net/tas/api}
      - TOPDESK_USERNAME=${TOPDESK_CUSTOM_USERNAME:-api_aipilots}
      - TOPDESK_PASSWORD=${TOPDESK_CUSTOM_PASSWORD:-7w7j6-ytlqt-wpcbz-ywu6v-remw7}
      - BEARER_TOKENS=${TOPDESK_CUSTOM_BEARER_TOKENS:-["e3707c16425c14fa417e2384a12748c0c7c51dfdfd1714c58992215983f33257"]}
      - MCP_HOST=0.0.0.0
      - MCP_PORT=3003
      - MCP_LOG_LEVEL=${LOG_LEVEL:-INFO}
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
          cpus: "0.5"
    
    # Enhanced Security Configuration
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Read-only root filesystem for security
    read_only: true
    
    # Drop all capabilities and add only necessary ones
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    
    # Temporary filesystems for writes
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=100m
      - /app/logs:rw,noexec,nosuid,nodev,size=50m
    
    # Health check
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3003/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # ElevenLabs Webhook Service
  elevenlabs-webhook:
    build: ./Servers/ElevenLabsWebhook
    container_name: elevenlabs-webhook
    restart: unless-stopped
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    # Internal network only
    networks:
      - mcp-internal
    
    # Environment configuration
    environment:
      - ELEVENLABS_WEBHOOK_SECRET=${ELEVENLABS_WEBHOOK_SECRET:-}
      - ELEVENLABS_WEBHOOK_HOST=${ELEVENLABS_WEBHOOK_HOST:-0.0.0.0}
      - ELEVENLABS_WEBHOOK_PORT=${ELEVENLABS_WEBHOOK_PORT:-3004}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FORMAT=${LOG_FORMAT:-text}
      - LOG_DIR=/var/log/mcp-services
      - LOG_FILENAME=webhook.log
      - ENABLE_AUDIO_STORAGE=${ENABLE_AUDIO_STORAGE:-false}
      - ENABLE_TRANSCRIPT_STORAGE=${ENABLE_TRANSCRIPT_STORAGE:-true}
      - AUDIO_STORAGE_PATH=/app/storage/audio
      - TRANSCRIPT_STORAGE_PATH=/app/storage/transcripts
      # TopDesk Integration
      - TOPDESK_URL=${TOPDESK_URL}
      - TOPDESK_USERNAME=${TOPDESK_USERNAME}
      - TOPDESK_PASSWORD=${TOPDESK_PASSWORD}
      # OpenAI Integration
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE=${OPENAI_API_BASE:-https://api.openai.com/v1}
    
    # Persistent storage for audio/transcripts and logs
    volumes:
      - elevenlabs-storage:/app/storage
      - ./logs:/var/log/mcp-services
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
          cpus: "0.5"
    
    # Enhanced Security Configuration
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Read-only root filesystem for security
    read_only: true
    
    # Drop all capabilities (port 3004 doesn't need NET_BIND_SERVICE)
    cap_drop:
      - ALL
    
    # Temporary filesystems for writes
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=100m
      - /app/logs:rw,noexec,nosuid,nodev,size=50m
    
    # Health check
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3004/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Voice Clone Pre-Call Service
  voiceclone-precall:
    build: ./Servers/VoiceClone_PreCall_Service
    container_name: voiceclone-precall
    restart: unless-stopped
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    # Internal network only
    networks:
      - mcp-internal
    
    # Environment configuration
    environment:
      - ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY}
      - ELEVENLABS_AGENT_ID=${ELEVENLABS_AGENT_ID}
      - ELEVENLABS_PHONE_NUMBER_ID=${ELEVENLABS_PHONE_NUMBER_ID}
      - ELEVENLABS_API_BASE=${ELEVENLABS_API_BASE:-https://api.elevenlabs.io/v1}
      - GREETING_VOICE_ID=${GREETING_VOICE_ID:-default_greeting_voice}
      - GREETING_MESSAGE=${GREETING_MESSAGE:-Hello thanks for calling. Please hold while we prepare your personalized experience.}
      - GREETING_MUSIC_ENABLED=${GREETING_MUSIC_ENABLED:-true}
      - GREETING_MUSIC_URL=${GREETING_MUSIC_URL}
      - CLONE_MAX_WAIT_SECONDS=${CLONE_MAX_WAIT_SECONDS:-35}
      - AUTO_TRANSITION_ENABLED=${AUTO_TRANSITION_ENABLED:-true}
      - DATABASE_URL=${VOICECLONE_DATABASE_URL:-postgresql+asyncpg://voiceclone:voiceclone@localhost:5432/voiceclone_db}
      - REDIS_URL=${REDIS_URL:-redis://localhost:6379/0}
      - CACHE_TTL=${CACHE_TTL:-86400}
      - VOICE_CLONE_TIMEOUT=${VOICE_CLONE_TIMEOUT:-30}
      - VOICE_SAMPLE_STORAGE=${VOICE_SAMPLE_STORAGE:-local}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME}
      - S3_REGION=${S3_REGION:-eu-west-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - LOCAL_VOICE_SAMPLES_PATH=/app/data/voices
      - 3CX_WEBHOOK_SECRET=${3CX_WEBHOOK_SECRET}
      - 3CX_TRUSTED_IPS=${3CX_TRUSTED_IPS:-127.0.0.1,10.0.0.0/8}
      - HOST=0.0.0.0
      - PORT=3006
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - WEBHOOK_SECRET=${VOICECLONE_WEBHOOK_SECRET}
      - CORS_ORIGINS=${CORS_ORIGINS:-["*"]}
    
    # Persistent storage for voice samples and logs
    volumes:
      - voiceclone-data:/app/data
      - ./logs:/var/log/mcp-services
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
          cpus: "0.5"
    
    # Enhanced Security Configuration
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Read-only root filesystem for security
    read_only: true
    
    # Drop all capabilities
    cap_drop:
      - ALL
    
    # Temporary filesystems for writes
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=100m
      - /app/logs:rw,noexec,nosuid,nodev,size=50m
    
    # Health check
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3006/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # DuckDNS IP updater service
  duckdns-updater:
    build:
      context: .
      dockerfile: Dockerfile.duckdns
    container_name: duckdns-updater
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    environment:
      - DUCKDNS_DOMAIN=${DUCKDNS_DOMAIN:-matrosmcp}
      - DUCKDNS_TOKEN=${DUCKDNS_TOKEN:-6fb5210e-71c6-4327-bbf9-af8283a08b37}
      - TZ=${TZ:-Europe/Amsterdam}
    
    networks:
      - web
    
    volumes:
      - duckdns-logs:/var/log:rw
    
    # Resource limits (very lightweight)
    deploy:
      resources:
        limits:
          memory: 32M
          cpus: "0.1"
        reservations:
          memory: 16M
          cpus: "0.05"
    
    # Health check
    healthcheck:
      test: ["CMD-SHELL", "pgrep crond > /dev/null || exit 1"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  # SSL Certificate Renewal Service
  ssl-renewer:
    build: ./nginx/ssl-renewer
    container_name: ssl-renewer
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=100m
      - /var/tmp:rw,noexec,nosuid,size=50m
      - /var/log/ssl-renewal:rw,noexec,nosuid,size=10m
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE  # Required for Let's Encrypt file operations
      - CHOWN        # Required for certificate file ownership
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Load centralized environment configuration
    env_file:
      - .env
    
    environment:
      - DOMAIN=${DOMAIN:-matrosmcp.duckdns.org}
      - TZ=${TZ:-Europe/Amsterdam}
      - COMPOSE_PROJECT_PATH=/compose
      - NGINX_CONTAINER=nginx-proxy
      - CERT_EXPIRY_DAYS=${CERT_EXPIRY_DAYS:-30}
    
    networks:
      - web
    
    volumes:
      # Let's Encrypt certificates (read-write for renewal)
      - /etc/letsencrypt:/etc/letsencrypt:rw
      # Docker socket for container restart capability
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Docker compose file for service management
      - ./docker-compose.yml:/compose/docker-compose.yml:ro
    
    # Resource limits (lightweight)
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: "0.5"
        reservations:
          memory: 64M
          cpus: "0.1"
    
    depends_on:
      - nginx-proxy

  # NGINX Reverse Proxy
  # Routes requests to all MCP servers
  nginx-proxy:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: unless-stopped
    
    networks:
      - mcp-internal
      - web
    
    # Expose only HTTPS to the world
    ports:
      - "80:80"   # For Let's Encrypt challenges
      - "443:443" # HTTPS
    
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro  # Let's Encrypt certificates
      - /var/www/certbot:/var/www/certbot:ro  # Let's Encrypt webroot
      - ./nginx/auth:/etc/nginx/auth:ro
      - nginx-logs:/var/log/nginx
    
    depends_on:
      calendar-mcp:
        condition: service_healthy
      # gmail-mcp:
      #   condition: service_healthy
      topdesk-custom-mcp:
        condition: service_healthy
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
        reservations:
          memory: 128M
          cpus: "0.25"
    
    # Enhanced Security Configuration
    security_opt:
      - no-new-privileges:true
      - apparmor:unconfined
    
    # Read-only root filesystem
    read_only: true
    
    # Drop all capabilities and add only necessary ones
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - CHOWN
      - DAC_OVERRIDE
      - SETGID
      - SETUID
    
    # Temporary filesystems for NGINX runtime
    tmpfs:
      - /var/cache/nginx:rw,noexec,nosuid,nodev,size=50m
      - /var/run:rw,noexec,nosuid,nodev,size=10m
      - /tmp:rw,noexec,nosuid,nodev,size=20m
    
    # Health check for NGINX
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  calendar-tokens:
    driver: local
  gmail-tokens:
    driver: local
  topdesk-data:
    driver: local
  nginx-logs:
    driver: local
  duckdns-logs:
    driver: local
  elevenlabs-storage:
    driver: local
  voiceclone-data:
    driver: local